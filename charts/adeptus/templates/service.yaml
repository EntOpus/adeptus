apiVersion: v1
kind: Service
metadata:
  name: {{ include "adeptus.fullname" . }}
  labels:
    {{- include "adeptus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "adeptus.selectorLabels" . | nindent 4 }}
